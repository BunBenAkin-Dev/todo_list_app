import 'package:flutter/material.dart';

class Listtray extends StatelessWidget {
  const Listtray({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView.builder(itemBuilder: (ctx, index) {});
  }
}
